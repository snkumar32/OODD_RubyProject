<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Course Manager</a>
    </div>
    <ul class="nav navbar-nav">
      <li class="active"><a href="#">Home</a></li>
    </ul>
    <div class="nav navbar-nav navbar-right">
    </div>
  </div>
</nav>
<div class = "container">
  <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
    <header class="masthead mb-auto">
      <div class="inner">
      </div>
    </header>
    <main role="main" class="inner cover">
      <h1 class="cover-heading">Welcome after teacher's log in page --- Landing Page</h1>
      <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'btn btn-success' %><span class="btn btn-lg btn-dark"></span>
    </main>
    <% if Teacher.exists?(email: current_user.email)
           teacher_id = Teacher.find_by(email: current_user.email)
      %>
        <%= link_to "View Courses", view_courses_path(id: teacher_id.id), :class => "btn btn-lg btn-info" %>
      <%end%>
  </div>
</div>